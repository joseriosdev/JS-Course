/*
Create the component
1- User Card
2- User Card Gallery


The Gallery Component should have a button that fetches a random user from https://randomuser.me/api and insert it into the gallery as a user-card component.

The user-card cmp should have a btn saying "show info" to display all the user's info and another btn that is "delete from gallery" action so it removes it from gallery.

The information should be saved as json in the local storage.

The user-card should display:
* name
* location
* gender
* picure
* email
* phone
* age
*/

